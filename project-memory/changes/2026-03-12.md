# 2026-03-12

## Retrieval Tags
- auth
- monitoring
- dashboard-shell
- content-json
- project-memory
- schema-compat

## Summary
- Reworked the app into a denser personal dashboard layout.
- Added a collapsible app sidebar and moved the dashboard into an authenticated app shell.
- Centralized visible UI text into JSON files under `/content`.
- Added Drizzle, PostgreSQL connection scaffolding, and core monitoring/auth schema.
- Implemented first-pass server-side website availability checks.
- Integrated Better Auth with login-only access flow.
- Disabled public sign-up and added a local admin creation script.
- Fixed Better Auth schema compatibility by changing auth table ids from `uuid` to `text` where required.
- Added local project memory rules and change-log workflow under `/project-memory`.
- Replaced the homepage demo grid with a single monitoring overview module backed by database site state.
- Added a real `/sites` page for site creation, frequency selection, and manual check triggers.
- Added per-site check interval storage with supported options of 10 minutes, 1 hour, and 6 hours.
- New active sites now run an initial check immediately after creation so dashboard status is populated right away.
- Reworked `/sites` into a top summary/action strip with an expandable site list.
- Added per-site detail expansion showing the latest HTTP check data and recent response-time history.
- Simplified availability checks to a single lightweight `GET` request and raised the degraded threshold to 2000ms.
- Added site deletion from `/sites`, using cascading deletes for related monitoring records.
- Added per-site star support so important sites can be prioritized in dashboard summaries.
- Simplified the homepage into colored status counts plus starred-site alert names only.

## Current Auth State
- Public sign-up is disabled.
- Login is available at `/login`.
- Admin users should be created with:
  - `npm run auth:create-admin -- --email you@example.com --password your-password --name Wayne`

## Current Monitoring State
- HTTP availability checks are implemented.
- Supports `HEAD` first, then `GET` fallback.
- API endpoints:
  - `/api/monitor/check`
  - `/api/monitor/check-all`
- Homepage now summarizes online vs abnormal sites from the `sites` table.
- `/sites` now manages check targets and stores `check_interval_minutes` per site.

## Follow-up
- Database should be synced again after auth schema id changes if not already updated.
- Database should be pushed again after adding `sites.check_interval_minutes`.
 - Next high-value step is adding a scheduler or cron runner that executes checks according to each site's configured interval.

## Privacy Note
- This note intentionally avoids private deployment details, real domains, credentials, and user-specific operational data.
