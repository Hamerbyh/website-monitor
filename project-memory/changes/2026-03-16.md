# 2026-03-16

## Retrieval Tags
- monitoring
- ssl
- tls
- issues
- sites

## Summary
- Added server-side TLS certificate checks for each monitored site using the configured site domain.
- Wired SSL checks into the existing site-level manual checks, batch checks, and due-site cron flow.
- Added environment-driven SSL cadence and expiry thresholds so certificate checks can run less frequently than HTTP checks.
- Persisted the latest per-site SSL state into `site_ssl_status` and synchronized SSL expiry issues into `issues`.
- Extended the `/sites` page to show certificate status, expiry timing, issuer, common name, and domain match state.
- Updated monitoring copy and configuration examples to reflect combined HTTP and SSL monitoring.

## Current Monitoring State
- Active sites now perform HTTP availability checks on their configured URL and periodic SSL checks on their configured domain.
- The due-site cron endpoint now reports how many SSL checks were executed during the batch.
- SSL checks currently default to port `443` and reuse the existing site-level monitor model.

## Follow-up
- If the database schema has not been applied yet in the target environment, run schema sync before expecting SSL state persistence.
- A next useful step would be extending email alerts to include SSL issue transitions in addition to HTTP status changes.

## Privacy Note
- This note intentionally avoids private deployment details, real domains, credentials, and user-specific operational data.
